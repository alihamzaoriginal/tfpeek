title = "tfpeek gitleaks config"

[allowlist]
description = "Allow a few known-benign strings that can appear in docs and CI config."
paths = [
  '''^CHANGELOG\.md$''',
]
regexes = [
  '''\$\{\{\s*secrets\.GITHUB_TOKEN\s*\}\}''',
]
